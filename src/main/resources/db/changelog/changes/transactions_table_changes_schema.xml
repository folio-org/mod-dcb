<?xml version="1.1" encoding="UTF-8" standalone="no"?>
<databaseChangeLog xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                   http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.4.xsd">
  <changeSet id="MODDCB-1" author="dbykov">
    <comment>
      In the scope of the story it was decided to rebuild the transactions table via
      changing columns identifications.
      The decision to drop off the initially designed table is completely secure,
      since the data population mechanism wasn't implemented, so the table has no content.
    </comment>
    <sqlFile path="changes/sql/2023-09-13_transaction_table_total_rebuild.sql" relativeToChangelogFile="true"/>
  </changeSet>
</databaseChangeLog>
